# Default configuration file
# This file is intended as a user-facing manual.
# Modify the values below according to your dataset, model, and evaluation setup.

device: auto
# Options:
#   auto  : use cuda if available, else mps if available, else cpu
#   cpu   : force CPU
#   cuda  : force NVIDIA GPU
#   mps   : force Apple Silicon GPU

data:
  name: Cobots_Synthetic
  # Options:
  #   MVTec
  #   Cobots_Synthetic
  #   Robotics_Hazards
  #   custom

  dataset_root: "D:\\DS\\AD\\Cobots_Synthetic"
  # Example:
  #   "/home/user/data/raw/mvtec_anomaly_detection"

  category: PLeft
  # For MVTec examples:
  #   bottle, cable, capsule, carpet, grid, hazelnut, leather,
  #   metal_nut, pill, screw, tile, toothbrush, transistor, wood, zipper

  # FOR Robotics_Hazards examples:
  #   corridor

  # FOR Cobots_Synthetic examples:
  #   PLeft, PRight, ConvBelt, RoboArm,

  img_size: 128
  batch_size: 16
  num_workers: 0
  pin_memory: false

  augmentation: light
  # Options:
  #   none
  #   light
  #   strong

model:
  name: vaegan
  # Options:
  #   vaegan

  checkpoint_root: "checkpoints"
  latent_dim: 64
  epochs: 1000
  encoder_lr: 0.0001
  decoder_lr: 0.0001
  discriminator_lr: 0.001
  pretrained: true
  # true  : load pretrained checkpoint if available
  # false : train from scratch

  recon_loss: BCE
  # Options:
  #   BCE
  #   MSE
  #   L1

  adversarial_loss: MSE
  # Options:
  #   MSE
  #   BCE

scoring:
  method: embedding_kmeans
  n_clusters: 8
  n_neighbors: 5
  metric: euclidean
  aggregation: mean
  # Options:
  #   embedding_kmeans
  #   embedding_knn

  quantiles: [0.5, 0.75, 0.9, 0.95, 0.99]
  # Used when the scoring method produces multiple score levels.

threshold:
  method: single_point
  # Options:
  #   single_point
  #   multi_point

  percentile: 95
  # Threshold is computed from normal training scores.
  # Common values:
  #   90, 95, 97.5, 99

  decision_rule: any
  # Used only with multi_point thresholding.
  # Options:
  #   any      : anomaly if at least one score exceeds the threshold
  #   all      : anomaly if all scores exceed the threshold
  #   majority : anomaly if more than half of scores exceed the threshold

output:
  dir: results/
  save_scores: true
  save_predictions: true
  save_metrics: true
  save_config: true
